{
  "name": "nucel-qa-action",
  "version": "1.0.0",
  "description": "Nucel QA GitHub Action runner",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"scripts/**/*.test.mjs\"",
    "lint": "node --check scripts/run-qa.mjs && node --check scripts/lib.mjs && node --check scripts/lib.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
